import { DashboardProps } from "../../interface/Dashboard.interface";

const Dashboard: DashboardProps = {
  title: "Schedule",
  basePath: "/schedule",
  icons: [{ name: "View employees", icon: "fas fa-users", path: "/employee" }]
};

export default Dashboard;
